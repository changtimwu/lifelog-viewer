# Optional. Without a key the app falls back to raster OpenStreetMap tiles
# (fine for local dev; OSM's tile server is NOT for production traffic).
# Get a free key at https://www.maptiler.com/ and drop it here as `.env`.
VITE_MAPTILER_KEY=
